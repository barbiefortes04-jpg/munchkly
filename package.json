{
    "name": "munchkly",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "echo 'No assets to build' && exit 0",
        "dev": "echo 'Development mode not supported' && exit 0"
    }
}