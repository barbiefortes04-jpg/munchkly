# Laravel Cloud Deployment Configuration
# Simplified configuration to avoid composer conflicts

# Build Configuration
BUILD_COMMAND="bash build.sh"

# Environment Configuration
APP_ENV=production
APP_DEBUG=false
LOG_CHANNEL=stderr
LOG_LEVEL=error

# Database Configuration (set by Laravel Cloud)
DB_CONNECTION=mysql

# Cache Configuration
CACHE_DRIVER=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis

# File Storage
FILESYSTEM_DISK=s3

# Laravel Optimization
CONFIG_CACHE_ENABLED=false
ROUTE_CACHE_ENABLED=false
VIEW_CACHE_ENABLED=false